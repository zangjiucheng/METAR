services:
  metar:
    image: ${METAR_IMAGE:-ghcr.io/zangjiucheng/metar:latest}
    container_name: metar
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      METAR_HOST: 0.0.0.0
      METAR_PORT: 8000
    restart: unless-stopped
